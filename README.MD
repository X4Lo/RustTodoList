# Rust Todo Lis

A simple Rust application to manage your tasks with basic CRUD operations.

## Features
- Add new tasks.
- Update task status (TODO, DOING, DONE).
- Remove tasks.
- View all tasks.

## Usage
1. Clone the repo.
2. Run `cargo run`.
3. Use the menu to manage tasks.

Tasks are stored in `tasks.txt`.

